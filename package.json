{
  "dependencies": {
    "pug": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "pug-cli": "^1.0.0-alpha6"
  },
  "scripts": {
    "build": "node build.js",
    "watch": "nodemon --watch . --ext pug --ignore 'includes/**' --ignore 'layout/**' --exec \"npm run build\""
  }
}